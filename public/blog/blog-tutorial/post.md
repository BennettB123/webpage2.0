# Blog Tutorial

Here's the steps to add a new blog entry:

- Add a folder for the post
- Put `post.md` inside it
- Add the post to `manifest.json`

> Real posts can live on the server instead of in this repo.
