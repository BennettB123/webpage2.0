# How to create blogs

Add a folder for the post, put `post.md` inside it, then add the post to `manifest.json`.

On the server, this content can live outside the app repo as long as it gets served from `/blog-content`.
